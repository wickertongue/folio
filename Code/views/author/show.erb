<h1>List of All Authors</h1>

<p>Below is a list of all Authors in the system. Please note that some Authors will not have been pinned to a book, and thus will not appear in the Inventory view.</p>

<table style="width 100%">
  <tr>
    <th>ID:</th>
    <th>Forename:</th>
    <th>Surname:</th>
  </tr>
    <% for each_item in @authors %>
    <tr>
      <td><%= each_item.id %></td>
      <td><%= each_item.forename %></td>
      <td><%= each_item.surname %></td>
    </tr>
    <% end %>
</table>
