<h1>Welcome to Folio</h1>

<p>Folio is an inventory management app for your bookstore. Listed below are all of the items you have in stock. Please click the 'more info' button next to each entry to view more information.</p>

<table style="width 100%">
  <tr>
    <th>Author</th>
    <th>Book Title</th>
    <th id="stock-column-header">Number in Stock</th>
    <th></th>
  </tr>
    <% for each_item in @inventory %>
    <tr>
      <td><%= each_item.present_author_or_authors %></td>
      <td><%= each_item.book.title %></td>

        <% case
        when each_item.quantity < 3 %>
        <td id="low-stock">
        <%= each_item.quantity %>
        </td>
        <% when each_item.quantity < 10 %>
        <td id="average-stock">
        <%= each_item.quantity %>
        </td>
        <% else %>
        <td id="good-stock">
        <%= each_item.quantity %>
        </td>
        <% end %>

        <td>
            <a href="/inventory/<%= each_item.id %>">
              <input class="btn btn-more_info" type="submit" value="More Info" />
            </a>
        </td>
    </tr>
    <% end %>
</table>
